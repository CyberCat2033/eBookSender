package com.cybercat.pocketbooksender.feature.catalog

import androidx.compose.ui.unit.dp

/** Duration of the selection checkbox slot expand/collapse animation. */
internal const val SelectionMotionDurationMillis = 220

/** Duration of a single catalog file removal (shrink + fade). */
internal const val RemovalMotionDurationMillis = 260

/** z-index of the sticky catalog section title row so it overlays scrolling content. */
internal const val CatalogSectionTitleZIndex = 1f

/** Animated width of the selection checkbox slot when edit mode is active. */
internal val SelectionSlotWidth = 36.dp

/** Size of the compact selection checkbox control. */
internal val SelectionControlSize = 24.dp
